              const SizedBox(height: 16),
              if (_loadingRoutes)
                const Padding(
                  padding: EdgeInsets.symmetric(vertical: 16),
                  child: Center(child: CircularProgressIndicator(color: JsColors.gold)),
                )
              else if (_routeComparisons.isNotEmpty) ...[
                Container(
                  padding: const EdgeInsets.all(14),
                  decoration: BoxDecoration(
                    color: JsColors.gunmetal,
                    borderRadius: BorderRadius.circular(10),
                    border: Border.all(color: JsColors.divider),
                  ),
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      const Text('💰 移動方法別の料金',
                          style: TextStyle(color: JsColors.gold, fontSize: 13, fontWeight: FontWeight.bold)),
                      const SizedBox(height: 10),
                      ..._routeComparisons.entries.map((e) => Padding(
                        padding: const EdgeInsets.only(bottom: 8),
                        child: Row(
                          mainAxisAlignment: MainAxisAlignment.spaceBetween,
                          children: [
                            Text(e.key.toUpperCase(),
                                style: const TextStyle(color: JsColors.silver, fontSize: 12)),
                            Column(
                              crossAxisAlignment: CrossAxisAlignment.end,
                              children: [
                                Text('${e.value.distance} / ${e.value.duration}',
                                    style: const TextStyle(color: JsColors.offWhite, fontSize: 12)),
                                if (e.value.estimatedGasCost != null)
                                  Text('約¥${e.value.estimatedGasCost}',
                                      style: const TextStyle(color: JsColors.gold, fontSize: 11, fontWeight: FontWeight.bold)),
                                if (e.value.fare != null)
                                  Text(e.value.fare!,
                                      style: const TextStyle(color: JsColors.gold, fontSize: 11, fontWeight: FontWeight.bold)),
                              ],
                            ),
                          ],
                        ),
                      )),
                    ],
                  ),
                ),
                const SizedBox(height: 16),
              ],
